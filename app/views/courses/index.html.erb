<p id="notice"><%= notice %></p>

<h1>Courses</h1>

<table border="1">
  <thead>
    <tr>
      <th>Course id</th>
      <th>Course name</th>
      <th>Duration</th>
      <th>Fees</th>
      <th colspan="2">Operations</th>
     
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%=course.c_id %></td>
        <td><%=course.c_name %></td>
        <td><%=course.duration %></td>
        <td><%=course.fees %></td>
        <!--<td><%= link_to 'Show Course Detail', course %></td>-->
        <td><%= link_to 'Edit Course', edit_course_path(course) %></td>
        <td><%= link_to 'Delete Course', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add Course', new_course_path %>
